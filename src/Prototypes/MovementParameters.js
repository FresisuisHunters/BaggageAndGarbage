function MovementParameters(startingNode) {
    this.previousNode = startingNode;   // Posicion del nodo en el que la maleta comenzo el movimiento
    //this.nextNode = endingNodePosition;       // Posicion del nodo al que se tiene que dirigir
    //this.lastDistanceToNextNode = 0;
}